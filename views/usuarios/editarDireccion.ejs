<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar dirección</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <!-- Navbar y alertas -->
    <%- include ../partials/navbar.ejs %>
        <%- include ../partials/alertas.ejs %>

            <div class="container mt-5">
                <h2 class="mb-4 text-center">Editar dirección</h2>

                <form action="/usuarios/editar-direccion/<%= direccion.id_direccion %>" method="POST"
                    class="row g-3 bg-light p-4 rounded shadow-sm">
                    <div class="col-md-6">
                        <label class="form-label">Teléfono</label>
                        <input type="text" class="form-control" name="telefono" value="<%= direccion.telefono %>"
                            required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Dirección</label>
                        <input type="text" class="form-control" name="direccion" value="<%= direccion.direccion %>"
                            required>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Ciudad</label>
                        <input type="text" class="form-control" name="ciudad" value="<%= direccion.ciudad %>" required>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Municipio</label>
                        <input type="text" class="form-control" name="municipio" value="<%= direccion.municipio %>"
                            required>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Estado</label>
                        <input type="text" class="form-control" name="estado2" value="<%= direccion.estado2 %>"
                            required>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Código Postal</label>
                        <input type="text" class="form-control" name="codigo_postal"
                            value="<%= direccion.codigo_postal %>" required>
                    </div>
                    <div class="col-12 text-end">
                        <button type="submit" class="btn btn-primary">Actualizar dirección</button>
                        <a href="/usuarios/direcciones" class="btn btn-secondary ms-2">Cancelar</a>
                    </div>
                </form>
            </div>


            <!-- Footer -->
            <%- include ../partials/footer.ejs %>

                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
                <script src="/js/notificaciones.js"></script>
</body>

</html>