// config/config.js
const config = {
    database: {
        host: 'localhost',
        user: 'root',
        password: 'admin',
        database: 'artesania', // Cambia según el nombre de tu base de datos
    }
};

module.exports = config;
